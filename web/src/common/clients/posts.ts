// Owner: feature/posts-core
// Dependencies: none (base client)

// Uncomment these imports when implementing the client
// import { createClient } from '@connectrpc/connect'
// import { createConnectTransport } from '@connectrpc/connect-web'

// === SERVICE CLIENT START ===
// Add service client initialization below this line
// This is the base client that other features may use

// const transport = createConnectTransport({
//   baseUrl: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8080',
// })

// export const postsClient = createClient(
//   PostService,
//   transport
// )

// === SERVICE CLIENT END ===

// === HOOKS START ===
// Add React Query hooks below this line

// export function useCreatePost() {
//   return useMutation({
//     mutationFn: async (input: CreatePostInput) => {
//       // Implementation
//     },
//   })
// }

// === HOOKS END ===
